{
    "type": "object",
    "properties": {
        "username": {
            "type": "string",
            "description": "The username of the user"
        },
        "password": {
            "type": "string",
            "description": "The password of the user"
        },
        "verificationCode": {
            "type": "string",
            "description": "The verification code received through two-step authentication"
        }
    },
    "required": ["username", "password", "verificationCode"],
    "additionalProperties": false
}